miscellaneous

1, nnstp is a python2 package to solve NPTSP using nearest neighbor algorihtm.

The only source file is "nntsp/nngraph.py".
You can delete other files (apart from "__init__.py") to maintain package structure.

2, "test.py" contain some example on how to use nntsp package.

3, "scorer_multiple.py" now takes in command line argument to specify how many \
instances to check score and where the instances are.